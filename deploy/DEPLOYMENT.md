# 🚀 Инструкция по настройке автоматического деплоя

## Настройка сервера

### 1. Подключитесь к серверу по SSH

```bash
ssh user@your-server-ip
```

### 2. Скопируйте скрипт настройки на сервер

```bash
# На вашем локальном компьютере
scp deploy/setup_server.sh user@your-server-ip:/tmp/
```

### 3. Запустите скрипт на сервере

```bash
# На сервере
sudo chmod +x /tmp/setup_server.sh
sudo /tmp/setup_server.sh
```

Скрипт попросит ввести:
- Путь для установки (по умолчанию: `/opt/poker-bot`)
- URL вашего GitHub репозитория
- BOT_TOKEN от @BotFather

## Настройка GitHub

### 1. Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите "New repository"
3. Введите название (например: `poker-club-bot`)
4. Выберите приватный или публичный репозиторий
5. Нажмите "Create repository"

### 2. Добавьте удаленный репозиторий

```bash
# В директории проекта на вашем компьютере
git remote add origin https://github.com/ваш-username/poker-club-bot.git
git branch -M main
git add .
git commit -m "Initial commit"
git push -u origin main
```

### 3. Настройте GitHub Secrets

1. Перейдите в репозиторий на GitHub
2. Откройте **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret** и добавьте следующие секреты:

#### Необходимые секреты:

| Secret Name | Описание | Пример |
|------------|----------|---------|
| `SERVER_HOST` | IP адрес или домен вашего сервера | `123.45.67.89` |
| `SERVER_USER` | SSH пользователь | `root` |
| `SERVER_PORT` | SSH порт (необязательно, по умолчанию 22) | `22` |
| `SSH_PRIVATE_KEY` | Приватный SSH ключ | См. инструкцию ниже |
| `DEPLOY_PATH` | Путь к боту на сервере | `/opt/poker-bot` |

### 4. Создание SSH ключа

Если у вас еще нет SSH ключа для деплоя:

```bash
# На вашем компьютере
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/poker-bot-deploy
```

**Скопируйте публичный ключ на сервер:**

```bash
ssh-copy-id -i ~/.ssh/poker-bot-deploy.pub user@your-server-ip
```

Или вручную:

```bash
# Показать публичный ключ
cat ~/.ssh/poker-bot-deploy.pub

# На сервере добавьте этот ключ в
nano ~/.ssh/authorized_keys
```

**Добавьте приватный ключ в GitHub Secrets:**

```bash
# Скопируйте содержимое приватного ключа
cat ~/.ssh/poker-bot-deploy
```

Скопируйте весь вывод (включая `-----BEGIN OPENSSH PRIVATE KEY-----` и `-----END OPENSSH PRIVATE KEY-----`) и добавьте как secret `SSH_PRIVATE_KEY`.

## Автоматический деплой

После настройки каждый push в ветку `main` будет автоматически:

1. ✅ Подключаться к серверу по SSH
2. ✅ Делать `git pull` последних изменений
3. ✅ Обновлять зависимости Python
4. ✅ Перезапускать бота через systemd

## Проверка деплоя

### Просмотр логов GitHub Actions

1. Перейдите в репозиторий на GitHub
2. Откройте вкладку **Actions**
3. Выберите последний workflow
4. Проверьте логи выполнения

### Просмотр логов бота на сервере

```bash
# Подключитесь к серверу
ssh user@your-server-ip

# Просмотр логов в реальном времени
sudo journalctl -u poker-bot -f

# Просмотр последних 100 строк логов
sudo journalctl -u poker-bot -n 100

# Проверка статуса
sudo systemctl status poker-bot
```

## Управление ботом на сервере

```bash
# Перезапуск бота
sudo systemctl restart poker-bot

# Остановка бота
sudo systemctl stop poker-bot

# Запуск бота
sudo systemctl start poker-bot

# Проверка статуса
sudo systemctl status poker-bot

# Просмотр логов
sudo journalctl -u poker-bot -f
```

## Ручное обновление на сервере

Если нужно обновить бот вручную:

```bash
cd /opt/poker-bot
git pull origin main
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart poker-bot
```

## Откат к предыдущей версии

```bash
cd /opt/poker-bot
git log --oneline  # Найдите нужный commit
git reset --hard <commit-hash>
sudo systemctl restart poker-bot
```

## Решение проблем

### Деплой не запускается

1. Проверьте, что все secrets добавлены правильно
2. Убедитесь, что SSH ключ корректный (скопирован полностью)
3. Проверьте, что сервер доступен по SSH

### Бот не запускается после деплоя

```bash
# Проверьте логи
sudo journalctl -u poker-bot -n 50

# Проверьте .env файл
cat /opt/poker-bot/.env

# Проверьте права доступа
ls -la /opt/poker-bot
```

### Git pull не работает

```bash
# На сервере проверьте git статус
cd /opt/poker-bot
git status

# Если есть конфликты, сбросьте изменения
git reset --hard origin/main
```

## Безопасность

⚠️ **Важно:**

1. Никогда не коммитьте `.env` файл в Git
2. Храните SSH ключи в безопасности
3. Используйте сильные пароли для сервера
4. Настройте firewall на сервере
5. Регулярно обновляйте систему и зависимости

## Дополнительные настройки

### Добавление webhook для мгновенного деплоя

Вместо ожидания GitHub Actions можно настроить webhook для мгновенного обновления.

### Использование Docker

Если хотите использовать Docker, можно модифицировать workflow для сборки и деплоя контейнера.

### Настройка мониторинга

Рекомендуется настроить мониторинг бота (например, UptimeRobot или Prometheus).

---

✅ После выполнения всех шагов ваш бот будет автоматически обновляться при каждом push в GitHub!
